// Configuração central de branding e prefixos
// Ajuste estes valores para migrar este template para outras disciplinas/projetos.

// Nome da disciplina/curso (ex.: "LPOO", "Algoritmos", "Estruturas de Dados")
window.APP_NOME_DISCIPLINA = window.APP_NOME_DISCIPLINA || "TGS";

// Sigla da disciplina (ex.: "LPOO", "ALG", "ED")
window.APP_SIGLA = window.APP_SIGLA || "TGS";

// Nome do professor/responsável (ex.: "Prof. Tiago Sombra")
window.APP_NOME_PROFESSOR = window.APP_NOME_PROFESSOR || "Prof. Tiago Sombra";

// Instituição/organização (opcional)
window.APP_INSTITUICAO = window.APP_INSTITUICAO || "Unichristus";

// Semestre/edição (opcional)
window.APP_SEMESTRE = window.APP_SEMESTRE || "2025.2"; // ex.: "2025.2"

// E-mail de contato (opcional)
window.APP_CONTATO_EMAIL = window.APP_CONTATO_EMAIL || "tiago.sombra@unichristus.edu.br";

// Prefixo para chaves no localStorage
// Dica: use algo curto e único por projeto (ex.: "lpoo", "alg", "spa")
window.APP_STORAGE_PREFIX = window.APP_STORAGE_PREFIX || "tgs";
